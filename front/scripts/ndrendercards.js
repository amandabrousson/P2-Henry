function renderCardsServer(data) {
    // Ejemplo de lógica de procesamiento en el servidor
    const processedData = data.map(movie => {
        return {
            
            // Puedes realizar otras transformaciones según sea necesario
        };
    });

    // Imprimir los resultados en la consola
    console.log(processedData);
}

function renderCards(data) {

    data.forEach(element => {
       
        `;
        
    
}

module.exports = renderCardsServer;